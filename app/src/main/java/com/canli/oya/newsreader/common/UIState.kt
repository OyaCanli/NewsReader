package com.canli.oya.newsreader.common

/**
 * Enum class for easily shuffling UI state between LOADING,
 * SUCCESS and ERROR states
 */
enum class UIState {
    LOADING,
    SUCCESS,
    ERROR,
    EMPTY
}